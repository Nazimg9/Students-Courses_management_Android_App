<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techforallnow.tech_explica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register_activity" />
        <activity android:name=".welcome_registered" />
        <activity android:name=".course_enrollment"></activity>
        <activity android:name=".Admin_panel" />
        <activity android:name=".add_new_course" />
        <activity android:name=".payment"></activity>
    </application>

</manifest>